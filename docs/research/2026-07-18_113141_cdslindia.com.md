```
CDSL (cdslindia.com) corporate-bond data — half-yearly issuer/outstanding report snapshots (HTML tables) and open, no-auth CSV master downloads for securitised debt & preference shares. Reference/issuance data (issued & outstanding amounts), not trades.

2026-07-18_113141 IST : Initial exploration — Reports.html index, IssuerReportDetails.aspx snapshot schema, CommonApp CSV download endpoints.
2026-07-18_113539 IST : Round-2 verify — probed CommonApp for a corp-bond master CSV; only SDI & PS exist (CB/Debt/NCD/Bond/… all 404). Corp bonds are HTML-snapshot only.
```

# CDSL — cdslindia.com (Corporate Bond Reports)

_Explored 2026-07-18. CDSL = Central Depository Services Ltd, one of India's two depositories._

## What it is
The depository's **corporate-bond reference & outstanding-amount** data. This is **issuance /
holdings data, not trades** — no prices, no transaction feed. Complements the issuance side of
the research with **amount issued vs amount outstanding** per ISIN.

## 1. Half-yearly issuer report snapshots
Index page: `https://www.cdslindia.com/corporatebond/Reports.html`

Links to per-date snapshot pages of the form:
```
https://www.cdslindia.com/CorporateBond/IssuerReportDetails.aspx?ReportDate=DDMMYYYY
```
- Two snapshots per financial year: **31 March** and **30 September**, going back to **Sep 2017**
  (FY2017-18) through **Mar 2026** — ~18 snapshots.
- Each page is a single large HTML table (~4.8 MB for the latest — thousands of rows), server-rendered
  ASP.NET (row ids like `IssuerReporttbl_*`). **Scrape = parse the `<table>` directly**; no CSV export on these.
- **Columns:** Sr. No. · Name of the Issuer · ISIN · Issuance Date (DD-MM-YYYY) · Maturity Date ·
  Coupon Rate (%) · Payment Frequency · Embedded option · **Amount issued (₹ cr)** ·
  **Amount outstanding (₹ cr)**.
- Value: the only easy source here for **outstanding amount per ISIN** over time (half-yearly series).

## 2. Open CSV master downloads (no auth, direct)
Fetchable with a normal browser User-Agent; return `text/csv`:
- **Securitised Debt Instruments (SDI):** `https://www.cdslindia.com/eservices/CommonApp/SDICsvDownload`
  (~258 KB). Columns: SECURITY TYPE, ISSUER NAME, ISIN, INSTRUMENT NAME, COUPON RATE, ISIN DESCRIPTION,
  ISSUE DATE, RED MAT DATE, RTA NAME, COMPANY ADD, CONTACT PERSON, REMARK RED DATE, FACE VALUE,
  INTEREST FREQ, PUT CALL OPTION, ISIN ST(atus)…
- **Preference Shares:** `https://www.cdslindia.com/eservices/CommonApp/PSCsvDownload` (~2.3 MB).
  Columns: ISSUER NAME, ISIN, INSTRUMENT NAME, ISSUE DATE, RED MAT DATE, RTA NAME, COMPANY ADD,
  ISIN SHORT NAME, FACE VALUE, ISIN STATUS.
- Pattern `eservices/CommonApp/<X>CsvDownload` — **probed round 2**: only `SDI` and `PS` exist.
  `CB`, `Debt`, `NCD`, `Bond`, `CorporateBond`, `CorpBond`, `DebtSecurities`, `Equity`, `MF` all
  return HTTP 404. So **corporate bonds have no CSV master here** — the `.aspx` snapshots are the
  only route for corp-bond issued/outstanding data.

## Scraping notes
- No JS required — both the report pages and CSVs are plain HTTP GET.
- Large payloads; cache locally and diff snapshots for time series.
- Be polite (ASP.NET origin). No API/pagination — it's whole-file downloads.

## Fit for research
- **Universe + outstanding amounts** (issued vs outstanding ₹ cr) per ISIN, half-yearly since 2017.
- Cross-reference ISINs with BondCentral (reference master) and NSE/BSE (trades).
